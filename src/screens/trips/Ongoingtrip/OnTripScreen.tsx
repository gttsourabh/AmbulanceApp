import TripCompletedScreen from './TripCompletedScreen';

/**
 * OnTripScreen has been merged into TripCompletedScreen.
 * Drivers arriving at hospital now directly view the unified TripCompletedScreen.
 */
export default TripCompletedScreen;